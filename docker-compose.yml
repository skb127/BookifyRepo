services:
  bookify.api:
    image: ${DOCKER_REGISTRY-}bookifyapi
    container_name: Bookify.Api
    build:
      context: .
      dockerfile: src/Bookify.Api/Dockerfile
    depends_on:
      - cert-generator
      - bookify-db
      - bookify-redis
      - bookify-idp
      - bookify-seq

  bookify-db:
    image: postgres:17
    container_name: Bookify.Db
    environment:
      - POSTGRES_DB=bookify
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - ./.containers/database:/var/lib/postgresql/data
    ports:
      - 5432:5432
      
  bookify-idp:
    image: quay.io/keycloak/keycloak:latest
    container_name: Bookify.Identity
    command: start-dev --import-realm
    environment:
      - KC_BOOTSTRAP_ADMIN_USERNAME=admin
      - KC_BOOTSTRAP_ADMIN_PASSWORD=admin
    volumes:
      - ./.containers/identity:/opt/keycloak/data
      - ./.files/bookify-realm-export.json:/opt/keycloak/data/import/realm.json
    ports:
      - 18080:8080
  
  bookify-seq:
    image: datalust/seq:latest
    container_name: Bookify.Seq
    environment:
      - ACCEPT_EULA=Y
      - SEQ_FIRSTRUN_ADMINPASSWORD=Password123!
    volumes:
      - ./.containers/seq:/data
    ports:
      - 5341:5341
      - 8081:80
  
  bookify-redis:
    image: redis:latest
    container_name: Bookify.Redis
    restart: always
    volumes:
      - ./.containers/redis:/data
    ports:
      - 6379:6379
    
  cert-generator:
    image: mcr.microsoft.com/dotnet/sdk:9.0
    container_name: Bookify.CertGen
    # Generate the certificate & grant the read permissions to everyone
    command: sh -c "dotnet dev-certs https -ep /https/aspnetapp.pfx -p Password123 && chmod 666 /https/aspnetapp.pfx"
    volumes:
      - ${HOME}/.aspnet/https:/https